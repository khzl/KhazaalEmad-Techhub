let haystack = "leetcode";
let middle = "leeto";

function FindIndexForFirstCharacter(str,middle)
{
    for(let index = 0;index < str.length;index++)
    {
        if (str.slice())
        return true;
        else
        return -1;
    }
}

console.log(FindIndexForFirstCharacter(haystack,middle));