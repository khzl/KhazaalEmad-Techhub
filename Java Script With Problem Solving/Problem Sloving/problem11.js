let array = [17,18,5,4,6,1];

function SwapindexArray(array)
{
    let Matrix = [];
    for (let i = 0; i < array.length; i++)
    {

        Matrix.push(array);
    }
    return Matrix;
}

let SwapNumber = SwapindexArray(array);
console.log(SwapNumber);